# UNERD Official Shop Store

Full-featured e-commerce marketplace generated by **STM Converter**. Built with Next.js 14 + Python FastAPI + PostgreSQL. Includes seller dashboard, buyer registration, shopping cart, Midtrans payment gateway, and Biteship shipping integration.

---

## Quick Start (Replit) - Recommended

### Step 1: Import Repository
1. Go to [replit.com](https://replit.com) and click **Create Repl**
2. Select **Import from GitHub**
3. Paste this repository URL
4. Click **Import from GitHub**

### Step 2: Add PostgreSQL Database
1. In your Repl, open the **Tools** panel (left sidebar)
2. Search for **PostgreSQL** and click **Add**
3. The `DATABASE_URL` environment variable will be set automatically

### Step 3: Add API Keys (Secrets)
Open the **Secrets** tab in your Repl and add the following:

| Secret Name | Description | Where to Get |
|---|---|---|
| `MIDTRANS_SERVER_KEY` | Midtrans server key for payment processing | [Midtrans Dashboard](https://dashboard.sandbox.midtrans.com) > Settings > Access Keys |
| `MIDTRANS_CLIENT_KEY` | Midtrans client key for Snap popup | Same as above |
| `MIDTRANS_IS_PRODUCTION` | Set to `true` for live payments (default: `false`) | Set manually |
| `BITESHIP_API_KEY` | Biteship API key for shipping rates & tracking | [Biteship Dashboard](https://biteship.com) > Settings > API Keys |
| `SESSION_SECRET` | Random string for JWT sessions | Auto-generated if not set |

### Step 4: Run
Click the **Run** button. The app will:
- Install all dependencies automatically
- Set up the database tables
- Seed product data from `backend/seed_data.json`
- Start the frontend (port 5000) and backend (port 8000)

---

## Quick Start (Local Development)

```bash
# 1. Clone the repository
git clone <this-repo-url>
cd <repo-name>

# 2. Set up PostgreSQL and create a database
# 3. Copy and edit environment variables
cp .env.example .env
# Edit .env with your DATABASE_URL and API keys

# 4. Run the application
bash start.sh

# 5. Open in browser
# Frontend: http://localhost:5000
# Backend API: http://localhost:8000/docs
```

---

## Architecture

```
+-------------------+     +-------------------+     +-------------------+
|   Next.js 14      |     |   Python FastAPI   |     |   PostgreSQL      |
|   Frontend        |<--->|   Backend API      |<--->|   Database        |
|   Port 5000       |     |   Port 8000        |     |                   |
+-------------------+     +-------------------+     +-------------------+
                                |           |
                    +-----------+           +-----------+
                    |                                   |
            +-------v-------+                   +-------v-------+
            |   Midtrans    |                   |   Biteship    |
            |   Payment     |                   |   Shipping    |
            +---------------+                   +---------------+
```

- **Frontend**: Next.js 14 with App Router, Tailwind CSS
- **Backend**: Python FastAPI with SQLAlchemy ORM
- **Database**: PostgreSQL (auto-setup on Replit)
- **Payment**: Midtrans Snap (sandbox & production)
- **Shipping**: Biteship (multi-courier Indonesia)

---

## Seller Login Credentials

| Field | Value |
|---|---|
| Email | `unerd.footwear@store.local` |
| Password | *(Displayed when the repository was generated)* |

> **Note**: You can change the password from the seller dashboard after logging in.

---

## Payment Gateway - Midtrans

### Setup

1. **Create Account**: Register at [Midtrans](https://midtrans.com)
2. **Get API Keys**:
   - **Sandbox** (testing): [dashboard.sandbox.midtrans.com](https://dashboard.sandbox.midtrans.com) > Settings > Access Keys
   - **Production** (live): [dashboard.midtrans.com](https://dashboard.midtrans.com) > Settings > Access Keys
3. **Add to Secrets/Environment**:
   - `MIDTRANS_SERVER_KEY` = Your Server Key
   - `MIDTRANS_CLIENT_KEY` = Your Client Key
   - `MIDTRANS_IS_PRODUCTION` = `false` (sandbox) or `true` (production)

### Sandbox Testing

Use these test credentials in sandbox mode:

| Payment Method | Test Details |
|---|---|
| Credit Card | Number: `4811 1111 1111 1114`, CVV: `123`, Exp: Any future date |
| BCA Virtual Account | Select BCA VA at checkout |
| GoPay | Scan the QR code shown |

### Midtrans Dashboard Configuration

In your Midtrans Dashboard > Settings > Snap Preferences:
- **Finish Redirect URL**: `https://your-domain.replit.app/orders`
- **Unfinish Redirect URL**: `https://your-domain.replit.app/checkout`
- **Error Redirect URL**: `https://your-domain.replit.app/checkout`

---

## Shipping - Biteship

### Setup

1. **Create Account**: Register at [biteship.com](https://biteship.com)
2. **Get API Key**: Dashboard > Settings > API Keys > Create New Key
3. **Add to Secrets**: `BITESHIP_API_KEY` = Your API key

### Supported Couriers

JNE | SiCepat | J&T Express | AnterAja | TIKI | Ninja Express | ID Express | POS Indonesia | Grab Express | GoSend

### Features

- **Area Search**: Autocomplete address search for accurate shipping
- **Multi-Courier Rates**: Compare shipping costs across all couriers
- **Shipment Creation**: Create shipping orders with tracking numbers
- **Real-time Tracking**: Track package status and location
- **Shipping Labels**: Generate and download shipping labels

---

## Features

### For Buyers
- Browse product catalog with categories, search, and filters
- View detailed product pages with images, variants, and descriptions
- Add to cart with variant selection (size, color, etc.)
- Checkout with Midtrans Snap payment popup
- Choose shipping courier and see real-time rates
- View order history and track shipments

### For Sellers
- Dashboard with sales overview and order management
- Product management: add, edit, delete products
- Upload product images (multiple per product)
- Manage product variants (size, color, model, etc.)
- Process orders and manage shipping
- View sales analytics

### Technical
- JWT-based authentication (buyer & seller roles)
- Responsive design (mobile & desktop)
- SEO-friendly with Next.js SSR
- RESTful API with auto-generated docs at `/docs`
- Image upload and management
- Database seeding from JSON

---

## Project Structure

```
├── frontend/                 # Next.js 14 application
│   ├── src/
│   │   ├── app/              # App Router pages
│   │   │   ├── page.tsx      # Home / Product catalog
│   │   │   ├── login/        # Buyer & seller login
│   │   │   ├── register/     # Buyer registration
│   │   │   ├── cart/         # Shopping cart
│   │   │   ├── checkout/     # Checkout with payment
│   │   │   ├── orders/       # Order history
│   │   │   ├── seller/       # Seller dashboard
│   │   │   └── change-password/
│   │   └── components/       # Reusable UI components
│   ├── public/images/        # Product & profile images
│   ├── package.json
│   └── tailwind.config.ts
├── backend/                  # Python FastAPI application
│   ├── app/
│   │   ├── main.py           # FastAPI entry point
│   │   ├── config.py         # Environment configuration
│   │   ├── database.py       # SQLAlchemy setup
│   │   ├── models.py         # Database models
│   │   └── routes/           # API route handlers
│   │       ├── auth.py       # Authentication (JWT)
│   │       ├── products.py   # Product CRUD
│   │       ├── cart.py       # Cart management
│   │       ├── payment.py    # Midtrans integration
│   │       ├── shipping.py   # Biteship integration
│   │       ├── orders.py     # Order management
│   │       └── upload.py     # Image uploads
│   ├── seed.py               # Database seeder
│   ├── seed_data.json        # Product & seller data
│   └── requirements.txt
├── data/
│   └── scraped_data.json     # Complete scraped data (all product details)
├── start.sh                  # Startup script
├── .replit                   # Replit configuration
├── replit.nix                # Nix dependencies
└── .env.example              # Environment variable template
```

---

## Data Files

### `backend/seed_data.json`
Contains seller info, credentials, and product catalog used to seed the database on first run.

### `data/scraped_data.json`
Complete scraped data from Shopee including all product details, variants, categories, pricing, ratings, sold counts, and seller information. This file preserves the full original data for reference.

---

## Environment Variables Reference

| Variable | Required | Default | Description |
|---|---|---|---|
| `DATABASE_URL` | Yes | *(auto on Replit)* | PostgreSQL connection string |
| `MIDTRANS_SERVER_KEY` | For payments | - | Midtrans server key |
| `MIDTRANS_CLIENT_KEY` | For payments | - | Midtrans client key |
| `MIDTRANS_IS_PRODUCTION` | No | `false` | Use production Midtrans |
| `BITESHIP_API_KEY` | For shipping | - | Biteship API key |
| `SESSION_SECRET` | No | *(auto-generated)* | JWT signing secret |
| `PORT` | No | `5000` | Frontend port |
| `BACKEND_PORT` | No | `8000` | Backend API port |

---

## API Documentation

Once the backend is running, visit `/docs` on port 8000 for interactive Swagger API documentation.

Key endpoints:
- `POST /auth/login` - Login (buyer/seller)
- `POST /auth/register` - Register buyer account
- `GET /products` - List all products
- `GET /products/{slug}` - Product detail
- `POST /cart/add` - Add to cart
- `POST /payment/create` - Create Midtrans payment
- `POST /shipping/rates` - Calculate shipping rates
- `GET /orders` - List orders

---

## Troubleshooting

### Database not connecting
- On Replit: Make sure PostgreSQL is added from the Tools menu
- Locally: Check `DATABASE_URL` in `.env` matches your PostgreSQL setup

### Payment not working
- Verify `MIDTRANS_SERVER_KEY` and `MIDTRANS_CLIENT_KEY` are set correctly
- Use sandbox keys for testing, production keys for live
- Check Midtrans Dashboard for transaction logs

### Shipping rates not loading
- Verify `BITESHIP_API_KEY` is set
- Check if the API key is active in Biteship Dashboard
- Use test/sandbox key for development

---

*Generated by [STM Converter](https://github.com) - Shopee-to-Marketplace Builder*
*93 products | Generated 2026-04-08*
